name: hyphenate
description: "Correct, fast TeX-pattern hyphenation for Dart: pattern compilation, word breaking and greedy line breaking, with no Flutter dependency."
version: 1.0.0
# TODO: point these at the repository before publishing.
# homepage:
# repository:
topics:
  - text
  - typography
  - hyphenation

environment:
  sdk: ^3.13.2

dependencies:
  characters: ^1.4.1
  meta: ^1.15.0

dev_dependencies:
  bench_press: ^0.3.0
  test: ^1.25.0

resolution: workspace
