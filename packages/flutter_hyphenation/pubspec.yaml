name: flutter_hyphenation
description: "HyphenText, a drop-in replacement for Flutter's Text that hyphenates long words correctly using TeX hyphenation patterns."
version: 1.0.0
# TODO: point these at the repository before publishing.
# homepage:
# repository:
topics:
  - text
  - typography
  - hyphenation

environment:
  sdk: ^3.13.2
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  hyphenation: ^1.0.0

dev_dependencies:
  bench_press: ^0.3.0
  flutter_test:
    sdk: flutter
