name: hyphenation_workspace
description: "Workspace root for the hyphenation and flutter_hyphenation packages. Not published."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.5.0

workspace:
  - packages/hyphenation
  - packages/flutter_hyphenation
  - example
  - packages/flutter_hyphenation/example
