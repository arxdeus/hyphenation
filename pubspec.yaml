name: flutter_hyphen
description: "HyphenText, a drop-in replacement for Flutter's Text that hyphenates long words correctly using the legacy engine dictionaries."
version: 0.1.0
# TODO: point these at the repository before publishing.
# homepage:
# repository:
topics:
  - text
  - typography
  - hyphenation
  - i18n

environment:
  sdk: ^3.13.2
  flutter: ">=1.17.0"

dependencies:
  characters: ^1.4.1
  flutter:
    sdk: flutter
  hyphen: ^0.4.0

dev_dependencies:
  bench_press: ^0.3.0
  flutter_test:
    sdk: flutter

